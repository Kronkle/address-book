// Remove user to favorites
var User = require('../newmockup/user');

module.exports = function(callback){

	console.log("In deleteContact");

	// Log contact removal to the console and end HTTP response
	callback();

}